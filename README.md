# CS122---The-Alarmist
For our team project, we are planning on creating an alarm GUI application, combining
features from both the mobile alarm app and Google Calendar. Currently, for Google and
Apple’s Calendar application, there is no official alarm that goes off to remind the user of an
event or task that needs to be completed. While the user can receive notifications from the
Calendar app, what happens if the user turns on Do Not Disturb and silences all notifications.
They could, in theory, set an alarm on the “Clock” app with a label of the task or event, however,
the alarm can only be set up 24 hours in advance. This is a massive drawback of the Clock app
because most meetings in the workplace and businesses are set up days or even months in
advance. Our project is to build an application from scratch that has the basic features from
both applications. An example of setting up an alarm involves the user selecting a date from the
calendar, which displays a popup window. In this popup, the user enters the event name and
chooses the time for the alarm to sound. Additionally, there is an optional section for the user to
schedule a reminder before the alarm, which can be helpful if extra time is needed to commute
to an event or meeting.

Regarding external libraries required for this project, we plan to use PySimpleGUI and
PyQt5 for the UI framework and SQL for the database query to store the reminders and its
attributes. If hosting and connecting the SQL server is too time consuming, a substitute would
be to use CSV files to store data. For datasets, we will either create our own calendar dataset
or use one from Kaggle. Some additional Python libraries that will be used are Pandas,
Request, and MSSQL. All these libraries mentioned should be sufficient to develop the core
functionality of both the front and back end of the application.

***Members:***

● Nathan Wong (nathan.s.wong@sjsu.edu)

● Fnu Hasham (fnu.hasham@sjsu.edu)
